import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.robertcarpart',
  appName: 'RobertCarPart',
  webDir: 'dist'
};

export default config;
